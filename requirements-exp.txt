# Optional dependencies for experiment orchestration (install in .venv)
distributed-shampoo
scipy
# vLLM (cu129, matches torch cu128): bash scripts/install_vllm.sh
# HF fallback (no vLLM): BACKEND=hf BATCH_SIZE=32 bash scripts/run_eval_smoke.sh 0
